# Optional. When set, Generate uses useUIStream against this URL
# instead of the offline SpecStream mock. The endpoint must stream
# SpecStream JSONL (RFC 6902 patches). Never required for bun run dev.
# VITE_JSON_RENDER_API=/api/generate
