# Server-only TypeSafe Jev / AI Gateway key for experimental_composeSpec.
# Never prefix with VITE_ — the browser must not see this value.
# bun run dev works with this blank (fixture SpecStream path).
JEV_API_KEY=

# Aliases accepted by the server resolver (upstream docs / playground):
# JEV_AI_GATEWAY_API_KEY=
# AI_GATEWAY_API_KEY=
